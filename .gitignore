# Dependencies
node_modules/

# Build outputs
.next/
out/
build/
dist/
*.tsbuildinfo
*.log
*.tmp
*.swp

# Environment (never commit secrets)
.env
.env.*
!.env.example

# Editors
.vscode/
.idea/

# System files
.DS_Store
Thumbs.db

# Coverage
coverage/
htmlcov/
.coverage

# Playwright (E2E)
test-results/
playwright-report/
blob-report/

# Scratch / one-off artifacts
scratch/

# NOTE: `drizzle/` is intentionally COMMITTED — versioned migrations (ADR 012).
# Do not ignore it. The journal + snapshots + SQL migrations are the schema's
# single source of truth for `drizzle-kit migrate` (see SETUP-LOCAL.md).

# Generic compiled artifacts
*.pyc
__pycache__/
*.o
*.obj
*.class
*.exe
*.dll
*.so
*.a
*.out
venv/
.venv/
.mypy_cache/
.pytest_cache/
.gradle/
target/
